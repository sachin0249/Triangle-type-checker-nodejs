# Triangle-type-checker-nodejs
Node.js CLI program to classify a triangle based on three input sides. Determines whether the triangle is equilateral, isosceles, scalene, and also checks if it is right-angled using Pythagoras theorem. Uses readline for user input.
